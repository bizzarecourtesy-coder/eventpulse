import React,{useState} from 'react';import{createRoot}from'react-dom/client';import{BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer,PieChart,Pie,Cell}from'recharts';import'./styles.css';
const api=import.meta.env.VITE_API_URL||'http://localhost:5000/api';const metric=[{label:'Attendance',value:'78%',note:'156 checked in'},{label:'Satisfaction',value:'4.6',note:'out of 5 rating'},{label:'Feedback rate',value:'64%',note:'100 responses'},{label:'NPS',value:'+42',note:'healthy advocacy'}];const demo={registered:200,checkedIn:156,attendanceRate:78,feedbackCount:100,feedbackRate:64,averageRating:4.6,speakerRating:4.4,nps:42,insight:'High attendance and a strong NPS point to a repeatable event format. Prioritize the most-requested workshop topics next time.'};function App(){const[event,setEvent]=useState({title:'Future of Product 2026',date:'2026-10-14',location:'Bengaluru',capacity:250});const[data,setData]=useState(demo);const[events,setEvents]=useState([event]);const[token,setToken]=useState(localStorage.token||'');const request=async(path,opts={})=>{const r=await fetch(api+path,{...opts,headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`,...opts.headers}});if(!r.ok)throw Error((await r.json()).message);return r.status===204?null:r.json()};const save=async()=>{if(!token)return alert('Connect with an organizer account first.');try{const e=await request('/events',{method:'POST',body:JSON.stringify(event)});setEvents([...events,e])}catch(e){alert(e.message)}};return <main><aside><div className="brand">◈ EventPulse</div><p>ORGANIZER WORKSPACE</p><a className="active">Overview</a><a>Events</a><a>Attendees</a><a>Feedback</a><a>Insights</a><footer>Plan smarter.<br/>Measure what matters.</footer></aside><section><header><div><span>EVENT INTELLIGENCE</span><h1>Make every event <em>count.</em></h1><p>Turn attendance and feedback into an actionable planning advantage.</p></div><button onClick={save}>+ Create event</button></header><div className="eventbar"><div><b>{event.title}</b><small>{event.date} · {event.location}</small></div><select><option>All events</option>{events.map(e=><option key={e._id||e.title}>{e.title}</option>)}</select></div><div className="metrics">{metric.map(m=><article key={m.label}><small>{m.label}</small><strong>{m.value}</strong><span>{m.note}</span></article>)}</div><div className="grid"><article className="chart"><div><small>ENGAGEMENT TREND</small><h2>Attendance through the day</h2></div><ResponsiveContainer width="100%" height={230}><BarChart data={[['09:00',42],['10:00',88],['11:00',130],['12:00',156],['13:00',143],['14:00',118]].map(([time,attendees])=>({time,attendees}))}><XAxis dataKey="time"/><YAxis/><Tooltip/><Bar dataKey="attendees" fill="#8bea5b" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></article><article className="insight"><small>PLANNING SIGNAL</small><h2>Your next best move</h2><p>{data.insight}</p><div className="score"><b>{data.nps>0?'+'+data.nps:data.nps}</b><span>Net Promoter Score<br/>from {data.feedbackCount} responses</span></div><button className="ghost">Explore insights →</button></article></div><div className="grid"><article className="form"><small>QUICK EVENT SETUP</small><h2>Add a future event</h2><input value={event.title} onChange={e=>setEvent({...event,title:e.target.value})} placeholder="Event title"/><input type="date" value={event.date} onChange={e=>setEvent({...event,date:e.target.value})}/><input value={event.location} onChange={e=>setEvent({...event,location:e.target.value})} placeholder="Location"/><button onClick={save}>Save event</button></article><article className="chart"><small>RESPONSE MIX</small><h2>Feedback sentiment</h2><ResponsiveContainer width="100%" height={180}><PieChart><Pie data={[{name:'Positive',value:68},{name:'Neutral',value:24},{name:'Negative',value:8}]} dataKey="value" innerRadius={48} outerRadius={76}>{['#8bea5b','#f4c95d','#f06a6a'].map(c=><Cell key={c} fill={c}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></article></div></section></main>}createRoot(document.getElementById('root')).render(<App/>);
